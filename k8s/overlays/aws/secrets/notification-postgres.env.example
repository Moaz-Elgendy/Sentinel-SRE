# Copy to notification-postgres.env. See citizen-postgres.env.example.
# The readiness probe expects `pg_isready -U app_user -d notification_service`.
POSTGRES_USER=app_user
POSTGRES_PASSWORD=CHANGE_ME_generate_a_random_password
POSTGRES_DB=notification_service
