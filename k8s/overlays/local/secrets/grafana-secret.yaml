# See k8s/overlays/local/secrets/citizen-postgres-secret.yaml for the note on real-secret handling —
# same demo-only placeholder value, not a real credential.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-secret
  namespace: citizen-portal
type: Opaque
stringData:
  GF_SECURITY_ADMIN_USER: admin
  GF_SECURITY_ADMIN_PASSWORD: sentinal
