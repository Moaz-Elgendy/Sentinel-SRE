apiVersion: v1
kind: Service
metadata:
  name: notification-postgres
  namespace: citizen-portal
spec:
  type: ClusterIP
  selector:
    app: notification-postgres
  ports:
    - port: 5432
      targetPort: 5432
