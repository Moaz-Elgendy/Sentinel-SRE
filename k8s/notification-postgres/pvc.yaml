apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notification-postgres-pvc
  namespace: citizen-portal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
