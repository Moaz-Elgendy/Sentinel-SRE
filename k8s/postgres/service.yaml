apiVersion: v1
kind: Service
metadata:
  name: citizen-postgres
  namespace: citizen-portal
spec:
  type: ClusterIP
  selector:
    app: citizen-postgres
  ports:
    - port: 5432
      targetPort: 5432
